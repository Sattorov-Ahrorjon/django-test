version: '3.7'

services:
  django:
    image: django-test:latest
    command: python manage.py runserver 0.0.0.0:8002
    container_name: django_web
#    volumes:
#      - .:/root/main
    ports:
      - "8002:8002"
